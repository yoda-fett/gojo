export default function AppSettingsPage() {
  return (
    <div>
      <h1 className="mb-6 text-2xl font-semibold text-[var(--color-dark-gray)]">App</h1>
      <p className="text-sm text-[var(--color-mid-gray)]">
        App preferences (theme, version, etc.) will live here. Coming soon.
      </p>
    </div>
  );
}
